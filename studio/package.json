{
  "name": "cronos-town-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Modular TypeScript re-architecture of Cronos Town (behavior-preserving migration).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^4.1.10"
  }
}
